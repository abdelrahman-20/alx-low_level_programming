#include <stdio.h>

/**
 * main - zyx
 *
 * Description: zyx
 *
 * Return: 0 Success
*/

int main(void)
{
	char ch = 'z';

	while (ch >= 'a')
	{
		putchar(ch);
		ch--;
	}
	putchar('\n');

	return (0);
}
